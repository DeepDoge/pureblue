ARG FEDORA_VERSION
ARG REMOTE_IMAGE_NAME_PREFIX

FROM ${REMOTE_IMAGE_NAME_PREFIX}-asus:latest

WORKDIR /tmp/pureblue-build
COPY ./nvidia ./nvidia

ENV FEDORA_VERSION=${FEDORA_VERSION}

RUN ./nvidia/setup.sh

RUN rm -rf /tmp/* /var/*
RUN ostree container commit